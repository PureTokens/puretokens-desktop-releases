# Beta channel

`latest.json` is created only when a complete signed prerelease is published.
Versions containing a SemVer prerelease suffix read this channel automatically;
there is no user-facing channel selector.
